### 1. 새로운 이슈 생성 (POST)
POST http://localhost:8080/api/issues
Content-Type: application/json

{
    "title": "HTTP Client Test",
    "description": "인텔리제이에서 바로 API 테스트하기",
    "projectId": 1,
    "reporter": 1
}